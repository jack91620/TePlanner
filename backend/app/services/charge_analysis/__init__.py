"""Phase A.4 — charge-analysis services.

`suggester` ports iOS ChargeLimitSuggester. Future analytics (per-week
energy added, average session duration, location frequency) live here
too so the iOS / Android / Harmony clients all consume one source.
"""
