<!--pages/webview/webview.wxml-->
<view class="container">
  <web-view
    wx:if="{{url}}"
    src="{{url}}"
    bindmessage="onMessage"
    bindload="onLoad"
    binderror="onError"
  />

  <view wx:if="{{loading}}" class="loading-overlay">
    <view class="loading-content">
      <view class="loading-spinner"></view>
      <text class="loading-text">Loading...</text>
    </view>
  </view>

  <view wx:if="{{error}}" class="error-container">
    <image class="error-icon" src="/assets/icons/error.png" mode="aspectFit" />
    <text class="error-text">{{errorMessage}}</text>
    <button class="retry-btn" bindtap="retry">Retry</button>
    <button class="back-btn" bindtap="goBack">Go Back</button>
  </view>
</view>
